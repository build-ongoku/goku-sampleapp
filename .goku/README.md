# Goku

## Directory: <your-app>/`.goku`

This directory contains any files or scripts that are internally used by `goku` to generate and or run the project. 

You should **not** modify anything in this directory.